type Student = {
    firstName : string,
    lastName : string,
    age: number,
    grades: Grade[];
}

type StudentRefactor = {
    firstName : string,
    lastName : string,
    age : number,
    subjects: Subject[];
}